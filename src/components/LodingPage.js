import React from "react";

const LoadingPage = (props) => (
  <div className="loading-page">
    <img className="loading-page-img" src="/images/loader.gif"/>
  </div>
);

export default LoadingPage;
